# سجل التعديلات - برنامج تحويل الكلام إلى نص

## الإصدار الحالي (2026-03-25)

### 1. إصلاح طريقة كتابة النص (text-output.js)
- **قبل**: استخدام PowerShell + `SendKeys` + `Ctrl+V` للصق النص — كان يكتب حرف "v" بدل اللصق
- **بعد**: استخدام Windows API `SendInput` مع `KEYEVENTF_UNICODE` لكتابة كل حرف مباشرة
- إضافة تأخير 5ms بين كل حرف لمنع تكرار الأحرف
- استخدام PowerShell مستمر (persistent) بدل إنشاء واحد جديد كل مرة
- تمرير النص العربي عبر Base64 (UTF-16LE) لتجنب مشاكل الترميز

### 2. إصلاح معالجة tokens من Soniox (soniox-client.js)
- **قبل**: كان يكتب النص فقط عند تأكيده (is_final) — تأخير كبير
- **بعد**: يكتب النص فوراً من tokens غير النهائية (non-final) للكتابة المباشرة
- إضافة throttling (120ms) لتجميع النصوص قبل إرسالها — يحسن الاستقرار
- إصلاح مشكلة تكرار النص عند تصحيح Soniox للكلمات
- إصلاح فقدان الكلمات عند التصحيح — الآن يكتب الحروف الإضافية بدل تجاهلها
- تصفية رموز `<end>` والنقاط التلقائية التي يضيفها Soniox
- إعادة تعيين تتبع النص عند نهاية كل جملة (utterance)
- تغيير `max_endpoint_delay_ms` من 500 إلى 3000 لتقليل النقاط الزائدة

### 3. تغيير اختصار لوحة المفاتيح (config.js)
- **قبل**: `Ctrl+Shift+L` — كان يتعارض مع برامج أخرى ولا يعمل
- **بعد**: `Ctrl+Alt+L` — يعمل بنجاح

### 4. إعادة تصميم الواجهة بالكامل
#### index.html
- إضافة شريط عنوان مخصص مع أزرار تصغير (−) وإغلاق (×)
- إضافة مؤشر مستوى الصوت (Canvas) يعرض تذبذبات الصوت
- إضافة زر بدء/إيقاف التسجيل في وسط النافذة
- إضافة نص حالة البرنامج

#### styles.css
- تصميم جديد بالكامل بألوان داكنة (#1a1a2e)
- شريط عنوان مخصص قابل للسحب
- زر تسجيل دائري مع تأثيرات gradient
- الزر يتحول للأحمر أثناء التسجيل
- أيقونة التسجيل تتحول من دائرة (بدء) إلى مربع (إيقاف)
- حجم النافذة: 420×260 بكسل

#### renderer.js
- إضافة تحليل الصوت (AnalyserNode) لعرض تذبذبات الصوت بشكل مرئي
- رسم مؤشر الصوت بألوان متدرجة على Canvas
- إضافة أحداث لأزرار التحكم (تصغير، إغلاق، تسجيل)
- عرض خط أفقي عند عدم التسجيل

### 5. دعم System Tray (main.js)
- إضافة أيقونة في منطقة الإشعارات (System Tray)
- عند الضغط على (−): النافذة تختفي وتظهر الأيقونة في البار
- عند الضغط على (×): البرنامج يُغلق نهائياً
- قائمة كليك يمين على الأيقونة: فتح البرنامج / بدء التسجيل / إغلاق
- النقر المزدوج على الأيقونة يفتح النافذة
- إضافة IPC handlers لأزرار النافذة والتسجيل

### 6. إضافة زر تسجيل من الواجهة (preload.js)
- إضافة `toggleRecording()` للتبديل بين التسجيل والإيقاف من الزر
- إضافة `minimizeWindow()` لتصغير النافذة
- إضافة `closeWindow()` لإغلاق البرنامج

### 7. أيقونة سطح المكتب
- إنشاء ملف `launch.vbs` لتشغيل البرنامج بدون نافذة أوامر سوداء
- إنشاء اختصار "Arabic STT" على سطح المكتب

### 8. إضافة تسجيل للأخطاء (Debug Logging)
- إضافة ملف سجل في مجلد Temp لتتبع الأخطاء
- تسجيل tokens القادمة من Soniox والنصوص المكتوبة

---

## ملفات المشروع
```
STT MODEL/
├── main.js              # العملية الرئيسية (Electron main process)
├── preload.js           # جسر التواصل بين main و renderer
├── package.json         # تبعيات المشروع
├── launch.vbs           # ملف تشغيل بدون نافذة سوداء
├── .env                 # مفتاح API (غير مرفوع على GitHub)
├── .gitignore           # ملفات مستثناة من Git
├── renderer/
│   ├── index.html       # واجهة المستخدم
│   ├── styles.css       # التصميم
│   └── renderer.js      # منطق الواجهة والميكروفون
└── src/
    ├── config.js        # إعدادات البرنامج
    ├── soniox-client.js # الاتصال بـ Soniox WebSocket API
    └── text-output.js   # كتابة النص باستخدام SendInput
```
